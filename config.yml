formats:
                      - json
                      - scss

path:

  html:                 index.html

  root:
    src:                src
    dist:               dist/assets

  tokens:
    src:                src/tokens/**/*.yml
    dist:
      json:             dist/assets/json
      scss:             dist/assets/scss/tokens

  clean:
                      - dist/assets/**/*

  css:
    dist:               dist/assets/css

  fonts:
    src:                src/fonts/**/*.{woff,woff2}
    dist:               dist/assets/fonts

  icons:
    src:                src/icons/**/*.svg
    dist:               dist/assets/icons

  scss:
    src:                src/scss/**/*.scss
    dist:               dist/assets/scss
    watch:
                      - src/scss/**/*.scss
                      - dist/assets/scss/partials/_icons-sprite.scss
                      - dist/assets/scss/tokens/_tokens.scss

settings:

  browserSync:
    cors:               true
    server:             dist/

  icons:
    mode:
      symbol:
        sprite:         icons
      view:
        render:
          scss:
            dest:       ../../scss/partials/_icons-sprite.scss
        sprite:         icons
        prefix:         .icon-%s

  scss:
    browsers:
                      - last 2 versions
    devSuffix:          -dev
    fontPath:
      dev:              ../fonts
      prod:             ~usitc-design-system/dist/assets/fonts
    includePaths:
                      - node_modules
                      - dist/assets/fonts
